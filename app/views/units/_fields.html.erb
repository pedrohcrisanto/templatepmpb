<%= render 'shared/errors', object: f.object %>

<div class="form-group">
  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control', placeholder: 'Nome'  %>
</div>

<div class="form-group">
  <%= f.label :address %>
  <%= f.text_field :address, class: 'form-control', placeholder: 'Endereço'  %>
</div>

<div class="form-group">
  <%= f.label :zip_code %>
  <%= f.text_field :zip_code, class: 'form-control', placeholder: 'CEP'  %>
</div>


<div class="form-group">
  <%= f.label :parent_id, "Unidade Pai" %>
  <%= f.select :parent_id, Unit.all.select {|unit| unit.id != f.object.id}.collect {|unit| [unit.name.titleize, unit.id]}, {include_blank: 'Selecione uma unidade'}, { class: 'form-control' } %>
</div>
